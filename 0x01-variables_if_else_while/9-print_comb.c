#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
	int a;

	while (c < 10)
	{
		putchar(48 + c);
		if (c != 9)
		{
			putcher(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
